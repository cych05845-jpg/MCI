# MCI 快速檢傷系統 — 後端介接說明（給資訊室）

前端：GitHub Pages（https://github.com/cych05845-jpg/MCI）
後端：Google Apps Script（Code.gs，隨附）＋ Google 試算表（已共享）
金鑰：全部存於 Apps Script「指令碼屬性」，程式碼內無任何機密。

## 一、資料儲存結構（Google 試算表）

### 分頁「檢傷紀錄」（每列一筆檢傷）
收件時間 | 抵達時間 | 大量傷患編號 | 病歷號/流水號 | 患者 | 性別 | 年齡 | 屬性 |
到院方式 | 活動狀態 | 意識 | GCS | 呼吸 | 心跳 | 收縮壓 | 舒張壓 | SpO2 | 體溫 | 體重 |
無法測量原因 | 摘要 | 判斷依據 | 依據全文 | 綜合評級 | 最終級數 | 修改分級 | 備註 |
人員 | 模式 | 去向 | 已轉錄 | 事件

### 分頁「傷患登錄」（每列一筆基本資料，大量傷患資料表）
收件時間 | 大量傷患編號 | 姓名 | 性別 | 生日 | 身分證/護照 | 國籍 | 地址/省市 |
家用電話 | 手機 | 緊急聯絡人 | 關係 | 聯絡電話 | 完成連繫 | 家屬已到 |
檢傷級數 | 傷情簡述 | 口述摘要 | 備註 | 登錄人員 | 去向 | 病歷號 | 事件 | 掛號建檔

### 關鍵欄位說明
- **大量傷患編號**：全系統主鍵，兩分頁以此串接（同一事件內唯一）
- **事件**：批次代號，預設為當日日期（如 20260907），同日多事件由管理者另定
- **去向**＝「作廢」：該筆為作廢紀錄，介接時請排除
- **已轉錄**＝V：檢傷端已人工轉錄至院內正式檢傷
- 收件時間為 Date 型別；抵達時間/生日可能被 Sheets 存成 Date，讀取時請自行格式化

## 二、建議介接方式（擇一）

### 方式 A：直接輪詢試算表（建議，最低耦合）
以 Google Sheets API（服務帳號）定時讀取兩分頁，
增量條件：`收件時間 > 上次抓取時間`，並排除 `去向='作廢'`。
院內僅需對外連線，無須開放任何入站服務。

### 方式 B：呼叫本系統 REST API（JSON）
Base URL：Apps Script 網頁應用程式 /exec 網址（另行提供）

GET 查詢（皆需 `key=<API_KEY>`）：
- `?action=list&event=<事件>&key=…` → 兩分頁清單（近300筆、進行中窗口）
  - 加 `&adminkey=<ADMIN_KEY>` 可查一年內＋`&from=YYYY-MM-DD&to=YYYY-MM-DD` 區間
- `?action=lookup&serial=<編號>&event=<事件>&key=…` → 單一傷患（檢傷＋登錄合併）
- `?action=events&key=…` → 事件清單

POST 寫入（Content-Type 任意，body 為 JSON，需 `"apiKey"`）：
- `{"mode":"…", …}` 模式一覽：檢傷寫入(預設)、register、disposition、mark、chartno、
  regmark、regchart、edit、void、setevent(需adminKey)、transcribe、polish、vitals_ocr、ping
- 回應皆為 `{"ok":true/false, …}`

### 方式 C：HIS 主動接收（未來）
院方提供 HTTPS endpoint 後，前端 ⚙ 設定可直接改指向，JSON 欄位同檢傷紀錄分頁。

## 三、權限模型
- API_KEY（一般）：讀寫進行中事件（近 N 天，屬性 EVENT_ACTIVE_DAYS，預設 3）
- ADMIN_KEY（管理）：一年內歷史事件、時間查詢、管理面板
- 一年以上資料 API 不提供；事件結案後由試算表選單「MCI系統→歸檔」搬移至獨立試算表

聯絡：急診 翁護理長
